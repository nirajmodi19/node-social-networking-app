<!DOCTYPE html>
<html>
<head>
	<title></title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
</head>
<body>
	<!-- <script>
		function toggle() {
			var element = document.getElementById('comment_section');

			if(element.style.display== "block") {
				element.style.display == "none";
			else
				element.style.display == "block";
		}
	</script> -->
	<div class="container">
		<div class="row">
			<form action="/comments/<%= returnedPost._id %>" id="comment_form" name="postComment<%= returnedPost._id %>" method="POST">
				<div class="form-group">
					<textarea class="form-control" name="post_body"></textarea>
				</div>
				<div class="form-group">
					<button class="btn btn-lg btn-default btn-info" name="postComment<%= returnedPost._id %>" value="Comment">Post</button>
				</div>
			</form>
		</div>
		<div class="row" style="margin-top: 20px;">
			<div class="card" style="width: 99%; padding: 10px; background-color: #fff" >
				<div class="card-block">
					<div class="container">
						<div class="row">
							<img class="img-circle" style="width: 50px;float: left; margin-right: 7px;" src="<%= newUser.profile_pic %>">
							<h3><%= newUser.name %></h3>
						</div>
						<div class="row">
							<h6 class="card-subtitile text-muted"><%= posts[i].date_added %></h6><br>
						</div>
						<div class="row">
							<h4 class="card-text"><%= posts[i].body %></h4>
						</div>
						<div class="row">
							<h4 class="card-subtitile" onclick="toggle<%= posts[i]._id %>()">Show Comments</h4>
						</div>
					</div>
				</div>
				<div id='toggleComment<%= posts[i]._id %>' style="display: block;">
					<iframe src="/comments/<%= posts[i]._id %>" id="comment_iframe"></iframe>
				</div>
			</div>
		</div>
	</div>
</body>
</html>